package com.ecommerce.mini.entity.enums;

/**
 * Enum representing the status of an order
 */
public enum OrderStatus {
    PENDING,
    PAID,
    PROCESSING,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
